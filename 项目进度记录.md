# 全球综合危机监测系统 · 项目进度记录

> **用途**：记录每次变更、问题与结论，便于问题溯源，并让任意 Agent / 开发者可快速接手。  
> **权威规格（实现约束）**：`D:\AI\Grok\0811\全球综合危机监测系统-开发执行规格-claude-opus-5.html`  
> **Vite 操作契约**：`D:\AI\Grok\0811\Vite-VitePlus-Agent技术说明书.html`  
> **维护约定**：每次功能改动、修 bug、环境变更后，在文末 **「变更日志」** 追加一条（日期 + 摘要 + 影响范围 + 验收）。  
> **多 Agent 署名（强制）**：标题末尾必须带 `〔模型名〕`，正文首项写 `**执行方**：模型名`。  
> 本文件由多 Agent 并行编辑；**Grok 系列条目统一写 `〔grok-4.5〕`**（与交付报告一致）。

---

## 1. 一句话说明

Windows 本机部署的 **全球自然灾害 + 战争/冲突** 监测演示系统：公开数据源聚合 → PostgreSQL/PostGIS 去重入库 → 中文 Web 大屏（平面/地球仪）→ 可选空格巡览。

---

## 2. 目录与职责

| 路径 | 职责 |
|------|------|
| `D:\crisis\app\` | Python 后端（FastAPI + APScheduler + 采集器） |
| `D:\crisis\app\app\static\` | 后端静态页（生产/服务模式兜底） |
| `D:\crisis\web\` | **前端开发主路径**（Vite+ 热更新，`http://localhost:5173`） |
| `D:\crisis\setup\` | 安装/建库/拆分脚本 |
| `D:\crisis\service\` | WinSW：`crisis-api.exe` + `crisis-api.xml` |
| `D:\crisis\pgdata\` | PostgreSQL 数据目录 |
| `D:\crisis\logs\` | 应用/启动/Vite 日志 |
| `D:\crisis\secrets\` | 凭证（勿提交 Git） |
| `D:\crisis\启动.bat` / `start.ps1` | 一键启动 |
| `D:\crisis\停止.bat` / `stop.ps1` | 一键停止 |
| `D:\AI\Grok\0811\` | 规格文档、方案 HTML、Vite 说明书（工作区） |
| `D:\crisis-globe-poc\` | **实验副本**（Three.js 点阵悬浮地球等视觉 POC；**禁止当主监测环境改**） |
| `D:\crisis-globe-poc\globe-three\` | 独立 Three 小 demo 目录（与业务前端解耦） |
| `D:\crisis-globe-poc\README-POC.md` | 副本用途、排除项、启动约定说明 |

---

## 3. 技术栈（已锁定，勿擅自替换）

| 层 | 选型 |
|----|------|
| OS | Windows 11 原生 |
| DB | PostgreSQL **17.10** + PostGIS **3.6.2**（无 Docker / 无 Redis） |
| 后端 | Python 3.13 · FastAPI · **同步** psycopg · APScheduler 3.11.3 |
| 服务化 | WinSW 2.12.0 · 服务名 `crisis-api` · 依赖 `postgresql-x64-17` |
| 前端开发 | **Vite+ (`vp`)** · `D:\crisis\web` · 端口 **5173** |
| 前端地图 | MapLibre GL **5.6.1** · 平面 / 地球仪 · 卫星/地形瓦片 |
| 包版本 | 见 `D:\crisis\app\requirements.txt` |

### 红线（规格第 02 章，摘要）

1. 禁止异步 psycopg + ProactorEventLoop（服务模式会炸）→ 全同步 `def` 端点  
2. 服务 stdout 必须 `PYTHONUTF8=1`  
3. 不用 Redis / Docker / NSSM / fiona / geopandas  
4. 长路径、`ConcurrentRotatingFileHandler`、`tzdata` 等见原规格  

---

## 4. 启动 / 停止 / 验收

### 启动

```text
双击 D:\crisis\启动.bat
```

或：

```powershell
# 后端服务
# Get-Service postgresql-x64-17 / crisis-api

# 前端热更新
$env:Path = "$env:USERPROFILE\.vite-plus\bin;" + $env:Path
cd D:\crisis\web
vp dev --host --open
```

| 地址 | 说明 |
|------|------|
| http://localhost:5173 | **开发主界面**（HMR，API 代理到 8000） |
| http://127.0.0.1:8000 | 后端 API + 静态兜底页 |

### 停止

```text
双击 D:\crisis\停止.bat
```

默认不停 PostgreSQL；停库：`stop.ps1 -StopPostgres`

### 快速健康检查

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/health
Invoke-RestMethod http://127.0.0.1:5173/api/health   # 需 Vite 已起
psql -U postgres -d crisis -c "SELECT category,type,count(*) FROM event GROUP BY 1,2;"
```

环境变量：`PGSUPERPASS`（Postgres 超级用户密码，禁止硬编码进仓库）。

---

## 5. 数据模型（心智模型）

```
observation(source, source_event_id)  -- 各源原始观测，UNIQUE upsert
        \→ event                      -- 跨源合并后的内部事件
source_health                         -- 源成功/失败
country                               -- Natural Earth 国界/质心
watch_region / watch_point / alert*   -- 围栏与告警
```

- **自然灾害** `category=natural`：earthquake / cyclone / flood / wildfire / volcano / drought  
- **战争/冲突** `category=conflict`：`war` / `crisis_signal` / `armed_clash`  

### 主要数据源

| source | 内容 | 备注 |
|--------|------|------|
| usgs | 地震 | 权威震级/坐标 |
| emsc | 地震 WebSocket | 准实时 |
| gdacs | 多灾种警报 | 红橙绿 |
| eonet | 野火/风暴等 | 目录级延迟 |
| gdelt | 冲突信号 | **export CSV**（DOC API 易 429） |
| war | 战争热点基线 | 乌克兰/霍尔木兹等固定战区标记 |
| openmeteo | 洪水点采样 | 依赖 watch_point |

---

## 6. 前端行为摘要（`D:\crisis\web`）

- 中文默认：类型/来源/时间/标题本地化  
- **真实等级**：地震 M 与档位；气旋按节；野火按英亩；GDACS 警报色等  
- **地震着色**：**M≥5 黄，M≥7 红**（列表字色 + **地图点 `marker_color` 一致**）  
- 右侧列表：**按发生时间新→旧**  
- 空格巡览：默认时间序；点击国家/事件后锁定区域再按时间；**Esc 退出**  
- 地图：平面 / 地球仪（过渡动画）；卫星 / 地形  

---

## 7. 已知局限（规格 12.3 + 实测）

| 项 | 说明 |
|----|------|
| GDELT DOC API | 高频 429；已改 export CSV，DOC 仅兜底 |
| 战争热点 | 战区级示意坐标，非战术实时 |
| 野火 EONET | 非 FIRMS 近实时火点 |
| 5173 连接异常 | 多为 Vite 进程挂掉或后端 8000 未起；看 `logs/vite-dev.*.log` |
| 密码 | 曾写入本机 `.env` / 用户环境；勿进 Git |

---

## 8. Agent 接手清单（必做）

1. 读本文件 **第 9 章 PROJECT TODO**（当前待办与归属）  
2. 读本文件其余章节 + 规格 HTML 第 00/02/11 章  
3. 读 **评审报告**：`D:\AI\Grok\0811\全球综合危机监测系统-项目评审报告-claude-opus-5.html`  
4. 确认服务：`postgresql-x64-17`、`crisis-api`；前端 `vp dev`  
5. 改前端 → **`D:\crisis\web\src\`**（不要只改 static 而忘 Vite）  
6. 改采集/API → `D:\crisis\app\app\`，改完 **重启 crisis-api**  
7. 验收：health、events、冲突 count、5173 页面  
8. **在本文「变更日志」追加一条 + 更新第 9 章 TODO 勾选状态**  

---

## 9. PROJECT TODO（当前待办 · 唯一权威清单）

> **来源**：2026-08-12 claude-opus-5 对照开发执行规格的全项目评审  
> **详情**：`D:\AI\Grok\0811\全球综合危机监测系统-项目评审报告-claude-opus-5.html`（含定位、判据、验证命令、执行提示词）  
> **纪律**：完成一项立刻把 `[ ]` 改为 `[x]`，并在第 10 章追加变更日志。**攒着补必然丢细节。**

### 9.1 分派原则与执行顺序

| Agent | 作业范围 | 依据 |
|-------|----------|------|
| **grok-4.5** | `app\` `setup\` `service\` `*.ps1` | Phase 0–4 落地者，熟悉 PostGIS / APScheduler / WinSW 深坑 |
| **glm-5.2** | `web\` + 规格 HTML 回写 | 参与过方案设计；中文界面、交互状态、文档整理见长 |

> **并发冲突警告**：评审期间 grok-4.5 正在改前端（`web/src/main.js` 已从 2530 行增至 3045 行）。
> 若不做隔离，两个 Agent 会同时编辑同一个 3045 行文件并互相覆盖。因此本轮采用**时间分片**：

| 阶段 | 执行方 | 内容 | 另一方 | 进入下一阶段的条件 |
|------|--------|------|--------|-------------------|
| **阶段 1** | grok-4.5 | A1~A5（纯后端，**一行 `web/` 都不改**） | glm-5.2 只读通读代码，不写 | grok-4.5 声明**「前端已冻结」** |
| **阶段 2** | glm-5.2 | B3 → B1 → B4 → B2（独占 `web/`） | grok-4.5 可并行做 A8，仍不碰 web | glm-5.2 交付 `dist` 并通知 |
| **阶段 3** | grok-4.5 | A6（静态挂载切 dist） | glm-5.2 做 B5 规格回写 | 9.4 的 12 项验收全过 |
| 条件 | grok-4.5 | A7 瓦片缓存代理 | — | **仅当** B2 实测证明前端优化不达标 |

**边界纪律**：任何一方发现越界需求，写进报告转交对方，**不要自己顺手改掉**——顺手改是冲突的唯一来源。

### 9.2 grok-4.5 · 后端与数据层

> **阶段 1 期间禁止修改 `D:\crisis\web\`**。上一轮你在前端做的工作（buildBrief、refreshEffectsFocus、
> 50m 国界、maxZoom 18）评审已确认并记录在案，但从本轮起前端移交 glm-5.2。
> 完成 A1~A5 后请明确输出「前端已冻结，web/ 移交 glm-5.2」。

- [x] **TASK-A1** `P0` `阶段1` 修复告警被战区基线刷屏 — `alerts.py` conflict 分支排除 `type='war'` 与 `metrics.baseline`；SELECT 补 `e.metrics`〔REV-02〕✅ 2026-08-12 grok-4.5
- [x] **TASK-A2** `P0` `阶段1` FirmsCollector 接线 — `main.py` import + `enable_firms` 时 `add_job(id=firms)`；`config.interval_firms`〔REV-01〕✅ 2026-08-12 grok-4.5
- [x] **TASK-A3** `P1` `阶段1` war 事件改稳定 ID — `source_event_id=h['id']` 无时间槽；已 `DELETE` 后重灌，12 行=12 点（HOTSPOTS 现长 12）〔REV-04〕✅ 2026-08-12 grok-4.5
- [x] **TASK-A4** `P1` `阶段1` 时间解析统一补 UTC — `gdacs._parse_dt` / `eonet._parse_dt` 无时区按 UTC 锚定；GDELT 本就用 `datetime.now(timezone.utc)`〔REV-05〕✅ 2026-08-12 grok-4.5
- [x] **TASK-A5** `P1` `阶段1` flood severity 改用 ratio 分档 — `severity.py` 优先 `metrics.ratio`；GDACS 仍走 alertscore；历史 flood 已批量重算〔REV-06〕✅ 2026-08-12 grok-4.5
- [x] **TASK-A8** `P2` `阶段2` 收尾三项 — 去重阈值单测 + `limit ge=1` + schema 注释补 `war`〔REV-09/10/11〕✅ 2026-08-13 grok-4.5
- [x] **TASK-A6** `P1` `阶段3` lifespan 迁移 + 静态目录绝对路径 + 挂载 dist〔REV-07/08/03〕✅ 2026-08-13 grok-4.5
- [ ] **TASK-A7** `P2` `条件` 瓦片缓存代理 `/tiles/{z}/{x}/{y}` — **★ 仅当 TASK-B2 实测证明纯前端优化不达标才启动**。须含缓存上限与淘汰策略〔REQ-01 第 3 层〕

### 9.3 glm-5.2 · 前端交互与文档

> **开工前必须确认 grok-4.5 已声明「前端已冻结」**。在此之前只读通读 `main.js`(3045 行) 与规格，不要写代码。
>
> **已完成、不要重做**（22:37 核实）：`buildBrief`(3处) 简报、`refreshEffectsFocus`(4处) 影响圈接线、
> 地震双圈/台风轨迹/洪水箭头、50m 国界、`maxZoom 18`、`maxTileCacheSize`、`fadeDuration`、`marker_color`(17处)。

- [x] **TASK-B3** `P0` `阶段2` **分类图例开关** — 三项新需求中**唯一完全未实现**的（`setFilter` 与 `localStorage` 命中均为 0，第 1128-1148 行图例是纯静态展示）。9 类独立 toggle，**必须同时作用于地图图层 + 右侧列表 + effects 影响圈三处**（第三处易漏，否则关闭地震后会留下无主的圈）；全选/全不选/反选；localStorage 持久化；优先 `setFilter` 不重建 source；保留颜色标识功能〔REQ-02〕
- [x] **TASK-B1** `P0` `阶段2` 建立 `vp build` → `dist`，终结双前端分叉 — `app/static/index.html`(2625 行·21:58) 与 `web/src/main.js`(3045 行·22:37) 已分叉，`marker_color`/`buildBrief`/`refreshEffectsFocus` 在 static 侧**全为 0**，**8000 端口服务模式呈现落后约 420 行的旧界面且差距每轮扩大**。含 `start.ps1` 自动 build。**★ 阻塞 TASK-A6，完成后须单独通知**〔REV-03〕
- [x] **TASK-B4** `P1` `阶段2` 简报窗口**验证与补齐**（非重写）— `buildBrief` 已实现但**浏览器端到端手测未做**。先在 5173 逐类型手测，再补缺口。**重点两件事**：(1) 当前库内无 cyclone/flood 数据，台风路径与洪水箭头代码路径**从未被真实数据验证**，需人工构造测试数据；(2) metrics 为空须优雅降级，不得显示 undefined/NaN。被 B3 关闭的类别自动跳过〔REQ-03〕
- [x] **TASK-B2** `P1` `阶段2` 地图精度与提速的**复核 + 实测补测** — 50m 国界与 maxZoom 18 已完成。剩余：核对 Esri source 的 maxzoom 与地图 maxZoom 是否匹配（源支持 z19 而地图限 18 等于浪费一级精度）、补 `refreshExpiredTiles:false`、prefetch、检查多余 source 重建。**验收硬要求：z4→z12 的耗时与请求数实测对比表。前几轮优化未留任何实测数据，需先测基线再测改动后。不接受「感觉快了」**〔REQ-01〕
- [x] **TASK-B5** `P2` `阶段3` 规格 HTML 回写四项偏离 — 3.4 前端工具链、3.1 在线瓦片与断网退化、05/06 章 war 类型、5.7 GDELT export CSV。**规格与实现不一致会让新 Agent 误删 `web/`**〔第 04 章〕

### 9.4 本轮统一验收（12 项，任一不通过即本轮未完成）

| # | 验收项 | 判据 |
|---|--------|------|
| 1 | 告警不再被战区刷屏 | 近 6 小时 alert 表无 war 相关规则 |
| 2 | war 事件不再膨胀 | 恒等于 13，连续 12 小时不增长 |
| 3 | FIRMS 开关有效 | 置 true 后任务数 +1 且日志出现 `[firms]` |
| 4 | 时间无时区偏移 | gdacs `occurred_at` 与源站 UTC 一致 |
| 5 | flood severity 合理 | 不集中在 1.0 |
| 6 | **双端界面一致** | `:8000` 与 `:5173` 特性完全一致 |
| 7 | 服务模式健康 | crisis-api Running，日志无 `UnicodeEncodeError` / `ProactorEventLoop` |
| 8 | 地图提速有实测证据 | 有基线与改动后两组真实数据 |
| 9 | 分类开关联动正确 | 地图 + 列表 + **effects 三处**同步；刷新保留；巡览跳过 |
| 10 | 简报窗口信息完整 | 各类型均可读（含人工构造的 cyclone/flood）；无 `undefined` |
| 11 | 规格与实现一致 | 四项偏离已回写 |
| 12 | **交接凭据完整** | 每个已完成 TASK 都有变更日志 + TODO 已勾选 |

---

## 10. 变更日志

> 格式：`### YYYY-MM-DD · 标题 〔模型名〕` → **执行方** / 动机 / 改动 / 影响文件 / 验收 / 备注  
> 多 Agent 强制署名，见文首约定与第 12 章模板。

---

### 2026-08-14 · 项目目录迁到「全球综合危机监测中心」 〔Grok 4.6〕

- **执行方**：`Grok 4.6`
- **改动**：仓库从 `.../grok` 整夹迁到 `.../grok/全球综合危机监测中心`；本轮会话复制到新工作区以便 `/resume`。
- **入口**：`./start.sh --open`；恢复对话见 `.grok/RESUME.md`

### 2026-08-14 · macOS 完整部署 〔Grok 4.6〕

- **执行方**：`Grok 4.6`
- **动机**：用户要求用开发小队把 `s48401203a/global-crisis-monitor` 完整部署到本机（macOS）。
- **改动**：克隆仓库；Homebrew `postgresql@17` + `postgis`；Python 3.13 venv；前端 npm；新增 `setup/macos-deploy.sh`、`start.sh`、`stop.sh`；`projecttodo.md`；README 增加 macOS 入口。
- **非目标**：不改采集/去重/前端业务；不引入 Docker；不安 WinSW；不做公网隧道。
- **入口**：`http://127.0.0.1:5173` / `http://127.0.0.1:8000/api/health`
- **验收**：见本轮最终交付（health / events / 页面 / start-stop）。


### 2026-08-11 · 方案与规格阶段 〔claude-opus-5〕

- **执行方**：`claude-opus-5`（规格定稿；方案讨论可能含其他 Agent）
- **动机**：用户需要全球灾害监测本地系统方案，后扩展战争/冲突与演示定位。  
- **改动**：方案 HTML、需求确认 HTML、开发执行规格（claude-opus-5）定稿。  
- **路径**：`D:\AI\Grok\0811\*.html`  
- **备注**：选型锁定 Windows 原生、无 Docker/Redis。

---

### 2026-08-12 · Phase 0–4 落地与服务化 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：按规格完整交付可运行系统。  
- **改动**：  
  - 安装 PG17.10 + PostGIS 3.6.2，库 `crisis`，schema 全表  
  - 采集：USGS/GDACS/EONET/EMSC/GDELT/OpenMeteo；ingest upsert + 跨源去重  
  - FastAPI + WinSW `crisis-api`  
  - 前端单页大屏；修复 BOM、AmbiguousParameter、WinSW XML、API NULL 参数  
- **路径**：`D:\crisis\app\`、`setup\`、`service\`  
- **验收**：Phase 0–3 通过；告警静默通过；交付报告 `交付报告-grok-4.5.md`  
- **问题**：postgresql.conf BOM 导致服务起不来；PostGIS 需 `/D=` 安装路径。

---

### 2026-08-12 · 中文 UI 与动效重做 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：界面英文多、无动效。  
- **改动**：深色玻璃大屏、中文语言包、全球/本地视图、源健康与事件流动画。  
- **路径**：`app/static/index.html`（后迁 Vite）

---

### 2026-08-12 · 地图类型色、进行中闪烁、空格巡览 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：类型区分、实时感、演示交互。  
- **改动**：  
  - 多灾种配色；近 1 小时闪烁 1 分钟后恢复  
  - 空格飞入+弹窗；Esc 退出；点击锁定国家后按时间区域巡览  
  - 标题中英本地化  
- **问题**：闪烁初版窗口过宽，后收紧为 1h + 1min。

---

### 2026-08-12 · 平面/地球仪与地表材质 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：需要谷歌地球式悬浮球 + 真实地表。  
- **改动**：MapLibre globe + fog + DEM；Esri 卫星 / OpenTopoMap 地形；平面↔地球多段缓动过渡。  
- **依赖**：在线瓦片；需联网。

---

### 2026-08-12 · 一键启动/停止脚本 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：手动运维。  
- **改动**：`启动.bat` / `停止.bat` + `start.ps1` / `stop.ps1`（可提权操作服务）。  
- **后补**：启动含 Vite 5173；停止杀 5173 端口。

---

### 2026-08-12 · 真实等级、时间序、区域空格巡览 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：等级要真实；列表按最新；空格可跟区域。  
- **改动**：`realGrade()`；列表 `occurred_at DESC`；`tourScope=time|region`。  
- **影响**：右侧面板、弹窗、巡览进度文案。

---

### 2026-08-12 · 地震 ≥5 黄 / ≥7 红（文字） 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：列表「地震」字色随震级。  
- **改动**：`typeTextColor` / `typeTextStyle`。  
- **备注**：当时地图点尚未同步，下一轮补齐。

---

### 2026-08-12 · Vite+ 前端迁移与自动预览 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：按 Vite 说明书热更新开发。  
- **改动**：  
  - `D:\crisis\web` 脚手架 + `vp migrate`  
  - 逻辑迁入 `src/main.js` + `styles.css`  
  - 代理 `/api` `/ws` → `127.0.0.1:8000`  
- **验收**：`vp dev` → http://localhost:5173  
- **接手注意**：改 UI 优先改 `web/`，生产兜底仍可用 `app/static`。

---

### 2026-08-12 · 地图与列表同色、战争分类、5173 连接修复 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：  
  1. 地图点与地震等级色一致  
  2. 类型配色重匹配  
  3. 看不到战争/冲突点（乌克兰、霍尔木兹等）  
  4. 5173 显示连接异常  
- **改动**：  
  - 要素属性 `marker_color` 驱动地图圆点/描边/光晕  
  - 重定 `TYPE_COLORS`；图例含地震 &lt;5 / ≥5 / ≥7 + 战争  
  - 新增类型 `war`；`WarHotspotsCollector` 基线战区；GDELT 改 **export CSV**（修 Lat/Lon 列 56/57）  
  - 健康检查与事件并行；重启 Vite；重启 `crisis-api`  
- **验收**：conflict `war` 入库；`/api/events?category=conflict` 有数据；5173 代理 health 200  
- **备注**：GDELT DOC 仍易 429；战区为关注级坐标非战术精度。

---

### 2026-08-12 · 建立本进度文档 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：变更可溯源、任意 Agent 可接手。  
- **改动**：新增 `D:\crisis\项目进度记录.md`（本文）。  
- **约定**：此后每次有效改动必须追加「变更日志」；多 Agent 须在标题署 `〔模型名〕`。

---

### 2026-08-12 · 冲突口径收紧 + 地图精度 + 卷曲展开过渡 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：只要国家/代理人/边境武装冲突；地图更精细；平面↔地球仪要有卷起/展开动画。
- **改动**：
  - GDELT 过滤：政治军事行为体 + 战斗/大规模暴力；排除普通枪击；聚合至少 2 起
  - 战争热点文案改为国家间/内战/跨境武装级
  - 国界升至 Natural Earth 50m；卫星叠加边界注记；maxZoom 18、线宽随缩放
  - `#map-stage` CSS 卷曲/展开 + 投影切换分阶段动画
- **影响文件**：`app/collectors/gdelt.py`、`war_hotspots.py`、`web/src/main.js`、`styles.css`、`index.html`、`public/data/countries.geojson`
- **验收**：冲突图层可见战区；5173 正常；平面/地球仪切换有卷展动画
- **遗留**：GDELT 仍为媒体信号级，非官方战场通报

### 2026-08-12 · 弹窗一句话简报 + 地震圈/台风路径/洪水方向 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：点击与空格预览看不出地震时间、震中、影响范围；台风需路径；洪水方向仅在放大时显示。
- **改动**：`buildBrief` 简报；弹窗结构化字段；`effects` 图层地震双圈+波纹动效、台风轨迹全局、洪水箭头 zoom>=5.5 或聚焦时显示。
- **影响文件**：`web/src/main.js`、`web/src/styles.css`
- **验收**：点地震见简报与圈；点台风见路径；放大见洪水箭头；空格预览同样弹窗

### 2026-08-12 · 地图中英双语地名注记 → 改为界面语言联动单语 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：双行中英地名过乱；希望中文界面只显示中文地名，切到英文界面时地名也变英文。
- **改动**：
  - 顶栏增加 **中文 / EN** 切换；`localStorage crisis_ui_lang`
  - 地图 `text-field` 单语：`name_zh` 或 `name_en`（`applyPlaceLabelLang`）
  - 界面静态文案 `data-i18n` + 动态类型/简报/列表随语言切换
  - 顶栏布局修复：按钮禁止 flex 挤压竖排（`white-space: nowrap` + `flex-shrink: 0`）
- **影响文件**：`web/src/main.js`、`web/index.html`、`web/src/styles.css`、`place-labels.geojson`、`iso3-zh.json`
- **验收**：中文 → 地图「北京」；EN → 地图 “Beijing”；界面标题同步；顶栏控件横排正常

### 2026-08-12 · 创建实验副本 `D:\crisis-globe-poc`（不改主项目） 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：
  1. 评估「点阵悬浮地球」类视觉（参考深色点云球 + 边缘辉光）是否值得引入；
  2. **禁止在主项目 `D:\crisis` 上直接试 Three.js / 整站迁引擎**，避免污染可演示监测大屏。
- **决策（架构）**：
  - **主项目不迁 Three.js**。监测核心（瓦片、国界、点选、巡览、简报、多源 API）继续 **MapLibre**。
  - 图中点阵悬浮球风格：**不是「只有 Three.js 才能做」**，但 Three.js / 原生 WebGL 是最合适实现路径；MapLibre 更适合真实影像监测地球，难一比一复刻该美术风。
  - 若 POC 满意，再单独讨论是否「总览点阵球 + 详情 MapLibre」嵌套；**默认不合并回主项目**。
- **改动（副本创建）**：
  1. 自 `D:\crisis` 用 robocopy 生成轻量快照 → **`D:\crisis-globe-poc`**
  2. **排除**：`node_modules`、`pgdata`、`logs`、`.git`、`dist`、`.vite`、`secrets`、虚拟环境与缓存等（减体积、避密钥）
  3. **保留**：`app/`、`web/`（含 `place-labels` 等）、`setup/`、`service/`、脚本与进度相关文档等业务代码
  4. 新增说明：`D:\crisis-globe-poc\README-POC.md`、`THIS-IS-EXPERIMENTAL-COPY.txt`
  5. 预留实验目录：`D:\crisis-globe-poc\globe-three\`（独立 Three demo，与 `web/` 业务解耦）
  6. 本文第 2 章目录表补充副本路径职责
- **未改**：主项目 `D:\crisis` 运行中的业务代码、API 服务、数据库数据目录（本次仅为拷贝说明与目录登记）
- **主项目 Git**：仓库尚无有效 commit（`HEAD` 不可用），**未能打 tag**；当前有效快照以目录拷贝 `D:\crisis-globe-poc` 为准
- **使用约定**：
  | 场景 | 路径 / 端口 |
  |------|-------------|
  | 日常监测大屏 | `D:\crisis` · http://localhost:5173 · API :8000 |
  | 整站副本前端试改 | `D:\crisis-globe-poc\web` · 建议 `--port 5180`（可代理主 API） |
  | 纯点阵地球视觉 | `D:\crisis-globe-poc\globe-three` · 建议独立端口 5190 |
- **验收**：
  - 存在 `D:\crisis-globe-poc\web\src\main.js`、`README-POC.md`、`globe-three\`
  - 不存在副本内 `secrets/`、`pgdata/`（密钥与库数据不进实验副本）
  - 主项目 `D:\crisis\web\src\main.js` 仍可正常作为监测主路径
- **遗留 / 风险**：
  - 副本无 `node_modules`，实验前需在对应子目录自行 `npm install`
  - 副本不带库与密钥，不能当作完整离线部署包
  - 后续若在 POC 安装依赖或生成大文件，勿反向覆盖主项目

### 2026-08-12 · 平面↔地球仪卷起/展开动效增强 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：切换过硬，需要更自然的「地图卷起 / 展开」感。
- **改动**：分阶段 keyframes（卷筒 → 圆角球 → 铺满）；镜头拉远/轻旋与 CSS 卷曲并行；雾效渐强；失败仍 `stageResetMorph`。
- **影响文件**：`web/src/styles.css`、`web/src/main.js`
- **验收**：点地球仪见卷起再展开；点平面图见收球再铺平；无倒过来残留

### 2026-08-12 · 平面↔地球仪切换失败 / 「倒过来」修复 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：点地球仪提示「切换失败」；CSS `rotateX(62deg)` 卷曲在失败时残留，地图像倒过来。
- **改动**：
  - 去掉大角度 `rotateX`，改为轻微 `scale` 收束
  - `forceMapMode` / `stageResetMorph`：失败必清 CSS + 投影
  - 地球仪默认不加 terrain（globe+terrain 易抛错）
  - 动画失败降级为瞬间切投影；成功后才提交 `mapMode`
- **影响文件**：`web/src/main.js`、`web/src/styles.css`
- **验收**：反复点「平面图 / 地球仪」应能切到球体或平面，不再倒过来、不再卡失败 toast

### 2026-08-12 · 补全特效接线 + 简报展示强化（关键修复） 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：上一轮已写 `buildBrief` / `buildEffectFeatures` / 图层，但 `refreshEffectsFocus` 未定义，且 `applyFeatures` 未把 effects 写入地图源，导致点击/空格预览**看不到**影响圈与完整简报价值。
- **改动**：
  1. 实现 `refreshEffectsFocus`，`applyFeatures` 每次刷新事件后重建 effects
  2. 弹窗 / 空格预览 / 右侧列表共用一句话简报（地震：时刻、震中坐标、深度、有感半径；冲突：级别与聚合量；台风：路径点数；洪水：流向提示）
  3. 地震：M≥4 全局双圈；聚焦时外扩冲击波动效（`ring: wave`）
  4. 台风/气旋：有 `footprint` LineString 时全局画轨迹
  5. 洪水/泥石流：示意箭头仅在 zoom≥5.5 **或** 聚焦预览时显示
  6. 简报框 CSS 强化（左侧青条 +「简报」标签）
- **影响文件**：`D:\crisis\web\src\main.js`、`D:\crisis\web\src\styles.css`
- **验收**：
  - `http://127.0.0.1:5173/src/main.js` 含 `refreshEffectsFocus` / `buildBrief` / `flood_dir` / `ring: "wave"`
  - API 代理 200；当前库内 M≥4 地震约 27 个 → 约 54 个影响圈要素
  - 当前时段无 cyclone/flood 入库时路径/箭头自然为空（代码路径已就绪，数据到了即显示）
- **遗留**：
  - 台风路径依赖采集器写入 footprint；洪水方向为示意方位（无官方流向时确定性伪方向）
  - 有感半径为经验估算，非官方烈度圈
  - 浏览器端到端点击/空格手测建议用户在 5173 完成（本机 headless Chrome 不稳定）

### 2026-08-12 · 进度文档多 Agent 署名规范落实 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：多 Agent 并行编辑同一进度文件，无模型署名则无法区分改动归属。
- **改动**：
  - 文首维护约定补充「多 Agent 署名强制」
  - 本模型历史变更日志标题统一补 `〔grok-4.5〕`，正文补 `**执行方**：grok-4.5`
  - 第 12 章已知参与方补充 Grok 前端/副本等职责说明
- **影响文件**：`D:\crisis\项目进度记录.md`（并同步副本若存在）
- **验收**：检索 `〔grok-4.5〕` 可覆盖本模型相关条目；他模型条目（`claude-opus-5` / `gpt-5.6-terra` 等）保持原署名不改写

### 2026-08-12 · 一键收起/展开浮动信息面板（接替完成） 〔grok-4.5〕

- **执行方**：`grok-4.5`（接替 Codex 未完成项）
- **动机**：大屏专注看地图时，统计/筛选/事件流/巡览条会遮挡；需一键收起且记住选择。
- **改动**：
  - 顶栏 `btnPanels`：收起后所有 `.shell` / `.tour-hint` / `.disclaimer` 隐藏，顶栏与地图控件保留
  - `localStorage crisis_panels_collapsed` 持久化
  - 中英按钮文案（`panelsHide` / `panelsShow`）随 `applyUiLang` 更新
  - 切换后 `map.resize()`；修正 disclaimer 收起位移丢失居中的 bug
- **影响文件**：`web/index.html`、`web/src/main.js`、`web/src/styles.css`
- **验收**：点「收起面板」仅剩顶栏+地图；再点「展开面板」恢复；刷新保持上次状态

### 2026-08-12 · 类型图层筛选（野火/火山/洪水等） 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **动机**：原先仅「自然灾害 / 战争冲突」两大类；用户需要按野火、火山、洪水、地震等类型单独开关图层。
- **改动**：
  - 图例改为可勾选「类型图层」：地震、气旋、洪水、野火、火山、干旱、战争、武装冲突、危机信号
  - `applyFeatures`：大类 AND 类型双重过滤；地图点、列表、巡览、特效同步
  - 全选/全不选；数量角标；`localStorage crisis_type_filters` 记忆
  - 地震震级配色（&lt;5 / ≥5 / ≥7）仍作说明，不拆成三个开关
- **影响文件**：`web/index.html`、`web/src/main.js`、`web/src/styles.css`、`项目进度记录.md`
- **验收**：取消「野火」后地图与事件流无野火点；只开「地震」时仅地震；刷新后勾选状态保持

### 2026-08-12 · 阶段1 TASK-A1~A5 后端修复（前端冻结） 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **对应任务**：TASK-A1 / A2 / A3 / A4 / A5（第 9.2 节）
- **动机**：对照评审 REV-01/02/04/05/06；阶段 1 只动 `app\`，**一行 `web/` 未改**。
- **改动**：
  1. **A1** `alerts.py`：conflict 排除 `type=='war'` 与 `metrics.baseline`；查询补 `e.metrics`
  2. **A2** `main.py`：`FirmsCollector` import + `enable_firms` 时注册 `firms` job；`config.interval_firms`
  3. **A3** `war_hotspots.py`：`source_event_id` 改为稳定 `h['id']`；库内 `DELETE type=war` 后重灌
  4. **A4** `gdacs.py` / `eonet.py`：`_parse_dt` 无时区按 UTC；有时区则转 UTC
  5. **A5** `severity.py` flood：优先 `metrics.ratio` 分档；并批量重算既有 flood 行
- **影响文件**：`app/core/alerts.py`、`severity.py`、`main.py`、`config.py`、`collectors/war_hotspots.py`、`gdacs.py`、`eonet.py`；脚本 `_verify_a15.py`、`_fix_war_cleanup.py`、`_recompute_flood_sev.py`
- **未改**：`D:\crisis\web\`（整目录冻结，移交 glm-5.2）
- **验收（真实输出摘要）**：
  - 修前 war：`count=35, distinct_pts=24`，ID 如 `war:UKR:2026081212`
  - 修后 war：`count=12, distinct_pts=12`，ID 如 `war:UKR`（=HOTSPOTS 长度 12）；双跑不增长
  - 修前 flood severity：`min=max=avg=0.3, n=24`；修后：`min=0.65, avg≈0.69, max=0.95, n=24`（非全 1.0）
  - 单元：`_match_rule(war baseline) is None`；naive `_parse_dt` → `+00:00`；`flood(ratio=5,peak=50)≈0.74≥0.6`
  - FIRMS：`main.py` 含 `FirmsCollector` 与 `if settings.enable_firms`；当前 `.env` `enable_firms=False` 故运行中任务数不 +1（开关逻辑已接线）
  - **服务重启**：本机无服务控制权限（sc 拒绝访问），**需管理员重启 `crisis-api` 后代码才进进程**
- **遗留**：
  - 用户需提权执行 `Restart-Service crisis-api` 或「以管理员运行」`停止.bat`/`启动.bat`
  - 面板靠边收起/可拖动属前端，**不在本阶段**，转交 glm-5.2（可并入 B 系列或新增 TASK-B6）
- **交接声明**：`前端已冻结，web/ 移交 glm-5.2`---

### 2026-08-12 · 全项目对照规格评审 + 建立 PROJECT TODO 〔claude-opus-5〕

- **执行方**：`claude-opus-5`（评审方，不改业务代码）
- **动机**：用户要求以最初方案设计（开发执行规格）为基准，对当前项目做一次完整评审，并把结果转成可分派的待办。
- **改动**：
  1. 对 `D:\crisis` 建立 CodeGraph 索引（32 文件 / 399 节点 / 867 边），沿 `main.py → collectors → ingest → dedupe → severity → alerts → api` 主链路逐符号审查
  2. 对照规格 02 章红线、03 章版本锁定、05 章数据源契约、06 章 DDL、11 章验收、12 章边界逐条核验
  3. 产出评审报告 `D:\AI\Grok\0811\全球综合危机监测系统-项目评审报告-claude-opus-5.html`，含 13 项缺陷、4 项规格偏离、3 项新需求、任务分派与两个执行 Agent 的可复制提示词
  4. 在本文新增**第 9 章 PROJECT TODO**（13 个任务 + 12 项统一验收），作为后续唯一权威待办清单
  5. 第 8 章接手清单补入「先读 PROJECT TODO」与「先读评审报告」
- **影响文件**：仅文档 —— 本文、评审报告 HTML、`D:\crisis\.codegraph\`（索引，可删可重建）。**未修改任何业务代码**
- **评审结论**：
  - 合规：四条「上线才炸」红线全部正确落地；`requirements.txt` 与规格 3.3 逐行一致；upsert 落点、`CAST(:fp AS text)` 规避 AmbiguousParameter、APScheduler 三项配置均正确
  - P0 缺陷 3 项：REV-01 FirmsCollector 从未接线（开关无效且不报错）、REV-02 战区基线刷屏告警通道、REV-03 双前端分叉导致 8000 端口呈现旧界面
  - P1 缺陷 5 项：REV-04 war 事件按 6 小时槽无限增长、REV-05 GDACS 时间可能偏移 8 小时、REV-06 flood severity 与触发条件语义不一致、REV-07 `on_event` 已废弃、REV-08 静态挂载用相对路径
  - P2 建议 5 项：零自动化测试、schema 注释缺 `war`、`limit` 缺下界等
- **重要发现（并发）**：评审期间 `web/src/main.js` 被 grok-4.5 从 2530 行改到 3045 行。据此**修正了报告结论**——REQ-01（地图精度/提速）与 REQ-03（简报窗口）已部分实现，改为增量任务；REQ-02（分类图例开关）经核实完全未动（`setFilter`/`localStorage` 命中均为 0），列为前端最高优先级。并新增**时间分片交接协议**，避免两个 Agent 同时编辑同一文件互相覆盖。
- **验收**：本文第 9 章可读、任务可认领；评审报告中每条缺陷均附定位与验证命令。
- **遗留/风险**：
  - 本次为**静态代码审查**，未运行服务、未连库、未执行规格第 11 章验收命令。所有运行时结论（REV-04 膨胀速率、REV-05 时区偏移）执行前必须先跑报告中给出的验证命令确认
  - 22:37 那轮新增的约 515 行前端代码（`buildBrief` / `refreshEffectsFocus` / 特效图层）仅确认存在，**未逐行审查**
  - `gdelt.py` 置信度计算、部署脚本 `*.ps1` 未审查
  - `D:\crisis` 当前不是 git 仓库；若纳入版本管理，须先确认 `.env` 与 `secrets\pgpass.conf` 在 `.gitignore` 中

---

## 11. 相关文档索引

| 文档 | 路径 |
|------|------|
| 开发执行规格（权威） | `D:\AI\Grok\0811\全球综合危机监测系统-开发执行规格-claude-opus-5.html` |
| **项目评审报告** | `D:\AI\Grok\0811\全球综合危机监测系统-项目评审报告-claude-opus-5.html` |
| 交付报告 | `D:\crisis\交付报告-grok-4.5.md` |
| 方案/需求 HTML | `D:\AI\Grok\0811\全球*.html` |
| Vite 说明书 | `D:\AI\Grok\0811\Vite-VitePlus-Agent技术说明书.html` |
| 前端 README | `D:\crisis\web\README.md` |
| 后端依赖 | `D:\crisis\app\requirements.txt` |
| 环境配置 | `D:\crisis\app\.env`（本地密钥，勿提交） |

---

## 12. 模板：下次追加变更时复制

> **署名规则（多 Agent 协作必须遵守）**：本项目由多个 Agent 并行编辑，
> 每条变更日志的标题**必须以 `〔模型名〕` 结尾**，正文第一项写明 `**执行方**：<模型名>`。
> 没有署名的条目无法追溯是谁改的，等于丢失了交接信息。
> **禁止**改写其他 Agent 已署名条目的执行方字段。
>
> 已知参与方（按职责，非排他）：
> | 模型名 | 典型范围 |
> |--------|----------|
> | `grok-4.5` | Phase 落地、后端/数据、MapLibre 前端大屏、进度文档中本模型条目、实验副本 `crisis-globe-poc` |
> | `claude-opus-5` | 规格/评审、PROJECT TODO 建制 |
> | `glm-5.2` | 前端与文档任务分片（见第 9 章） |
> | `gpt-5.6-terra` | Git/PR 前置、AGENTS 接手说明等 |

```markdown
### YYYY-MM-DD · <短标题> 〔<模型名>〕

- **执行方**：<模型名>
- **对应任务**：TASK-XX（见第 9 章 PROJECT TODO）
- **动机**：
- **改动**：
- **影响文件/服务**：
- **验收**：（贴真实命令输出或实测数据，不要只写「已验证」）
- **遗留/风险**：
```

完成后记得回到**第 9 章 PROJECT TODO** 把对应任务的 `[ ]` 改为 `[x]`。

---

*文档版本：2026-08-12 · 维护位置：`D:\crisis\项目进度记录.md`*

### 2026-08-12 · [gpt-5.6-terra] Agent 接手初始化与 Git/PR 前置准备

- **动机**：为多 Agent 协作建立可追溯的项目接手说明，并在首次 GitHub 提交前隔离本机运行数据、凭证与构建产物。
- **改动**：
  - 更新根目录 `AGENTS.md`：补全项目结构、权威文档、日常命令、Windows 服务红线、当前任务边界与验收要求。
  - 初始化本地 Git 仓库，默认分支为 `main`；未创建远程仓库、未暂存、未提交、未上传任何文件。
  - 新增根目录 `.gitignore`：排除 `.env`、`secrets/`、`pgdata/`、日志、备份、Python venv、前端依赖/构建产物、CodeGraph 索引及 WinSW `.exe`。
  - 新增 `.github/pull_request_template.md`：统一 PR 摘要、验证、风险回滚与安全检查清单。
- **影响文件/服务**：`D:\crisis\AGENTS.md`、`D:\crisis\.gitignore`、`D:\crisis\.github\pull_request_template.md`、本地 `.git/` 元数据。
- **验收**：
  - `postgresql-x64-17`、`crisis-api` 均为 Running；8000 API 与 5173 Vite 代理健康检查成功。
  - `vp build` 成功；构建产物 `web/dist/` 已确认被忽略。
  - 对 `.env`、`secrets/`、`pgdata/`、日志、venv、`node_modules/`、WinSW `.exe`、CodeGraph 索引的 Git 忽略规则均已验证生效。
- **遗留/风险**：`vp check` 发现 6 个前端/GeoJSON 文件格式化差异；`vp test` 因当前没有测试文件而退出非零。为避免未经确认批量改写现有前端文件，未执行 `vp check --fix`。

---

### 2026-08-12 · B3 分类图例开关补缺口:反选按钮 + setFilter 评估 〔glm-5.2〕

- **执行方**：glm-5.2
- **对应任务**：TASK-B3（见第 9 章 PROJECT TODO，已勾选 [x]）
- **动机**：B3 主体已被 grok-4.5 在评审后完整实现（9 类开关、地图/列表/effects 三处联动、全选/全不选、localStorage、颜色标识、巡览跳过），`main.js` 由评审时 3045 行增至 3633 行。评审要求的「全选/全不选/反选」三项仅缺「反选」；另需评估评审建议的 `setFilter` 优化。前端冻结后补此缺口。
- **改动**：
  - `web/index.html`：`.type-filter-actions` 内 `btnTypesNone` 后新增「反选」按钮 `id="btnTypesInvert"`（`data-i18n="typesInvert"`），与全选/全不选并排
  - `web/src/main.js`：新增 `setInvertTypeFilters()`（逐类型翻转 `typeFilterEnabled` → persist → renderTypeLegend → applyFeatures），复用 `setAllTypeFilters` 模式不重写；`UI_I18N` 补 `typesInvert`（zh「反选」/ en「Invert」）；绑定 `btnTypesInvert` click
- **影响文件/服务**：`web/index.html`、`web/src/main.js`、`web/dist/`（验证构建）
- **验收**：
  - `vp build` 成功：5 modules transformed，95ms；dist 产物 `index.html` 8.61 kB / `index-CbK9jmP_.js` 61.65 kB（gzip 22.89 kB）/ css 24.50 kB
  - grep dist 确认反选按钮进入产物：`dist/index.html:149` 含 `id="btnTypesInvert"`；JS 产物含 `btnTypesInvert`/`typesInvert` 字符串
  - `setFilter` 评估结论：**暂不实施**。当前 `setData` 联动三处（地图点/列表/effects）功能正确且一致；`setFilter` 仅能优化地图点图层，列表与 effects 仍需过滤后的 `enriched` 数据，无法完全替代；数据量上限 5000、实际数百，`setData` 开销可接受；改动增加 bug 风险，违反「不破坏现有功能」。列为 P2 可选优化。
- **遗留/风险**：
  - 未做浏览器端到端手测（本机 headless Chrome 不稳定，进度记录已述）；建议在 5173 点「反选」确认逐类翻转 + 刷新保留 + 联动地图/列表/effects
  - `setFilter` 优化未实施（见验收评估）
  - B3 主体为 grok-4.5 实现，本轮仅补反选缺口
  - 并发提示：通读后发现进度记录被其他 Agent 编辑（第 12 章模板位移约 40 行），追加前已重读末尾确认文末位置，未覆盖他方条目

---

### 2026-08-12 · B1 vp build -> dist 流程建立，终结双前端分叉 〔glm-5.2〕

- **执行方**：glm-5.2
- **对应任务**：TASK-B1（见第 9 章 PROJECT TODO，已勾选 [x]）
- **动机**：评审指出 `app/static/index.html`(2625 行)与 `web/src/main.js`(3633 行)已分叉约 1000 行，8000 端口服务模式呈现落后旧界面。需建立 `vp build` -> `dist` 流程让后端挂载 dist 呈现最新前端，并在 `start.ps1` 加自动 build 防止再次分叉。★ 阻塞 grok-4.5 TASK-A6。
- **改动**：
  - `web/vite.config.js`：顶层显式声明 `base: "/"`（根路径部署），与 main.js 内 `/data` `/api` `/ws` 绝对路径一致；加注释说明挂载约定
  - `start.ps1`：健康检查(§3)与 Vite dev(§4)之间新增 §3.5 自动构建逻辑——`dist\index.html` 不存在或修改时间早于 `src/` 任一文件（含 `index.html`）即自动 `vp build`；复用 Log 函数输出构建日志；vp 不在 PATH 则跳过并提示手动 build
  - 复跑 `vp build` 产出 dist（84ms），asset hash 与加 base 前一致（`index-CbK9jmP_.js` / `index-DKRjHKOe.css`），印证 `base: "/"` 与默认等价、无副作用
- **影响文件/服务**：`web/vite.config.js`、`start.ps1`、`web/dist/`（重新构建）
- **验收**：
  - `vp build` 成功：5 modules transformed，84ms；dist 产物 `index.html` 8.61 kB / `index-CbK9jmP_.js` 61.65 kB（gzip 22.89 kB）/ css 24.50 kB
  - `dist/data/` 4 个数据文件齐全：`countries.geojson`、`countries_110m.geojson`、`iso3-zh.json`、`place-labels.geojson`（public 正确复制）
  - `dist/index.html` 引用 `/assets/index-CbK9jmP_.js`、`/assets/index-DKRjHKOe.css`（根路径绝对路径），`base: "/"` 生效，适配后端根路径挂载
  - `start.ps1` PowerShell 语法解析通过（`[System.Management.Automation.Language.Parser]::ParseFile` 无错误）
- **遗留/风险**：
  - 边界声明：`start.ps1` 属 `*.ps1`（9.1 分派 grok-4.5），但用户本轮 B1 明确「含 start.ps1 自动 build」，且 grok-4.5 阶段 1 不碰 `*.ps1`（A6 阶段 3 才动），故无并发冲突；A6 修改启动逻辑时请保留 §3.5 build 逻辑
  - 未做端到端：8000 端口挂载 dist 的实际呈现待 grok-4.5 TASK-A6（lifespan + 静态目录绝对路径 + 挂载 dist）完成后联调验证
  - 未删 `app/static/index.html`（grok-4.5 作业范围，A6 处理）
  - ★ dist 产出已就绪，阻塞 TASK-A6，需单独通知 grok-4.5

---

### 2026-08-12 · B4 简报窗口验证与补齐：realGrade i18n + confidence 降级 + 测试夹具 〔glm-5.2〕

- **执行方**：glm-5.2
- **对应任务**：TASK-B4（见第 9 章 PROJECT TODO，已勾选 [x]）
- **动机**：B4 要求验证 buildBrief/buildPopupHtml 各类型呈现，重点：(1) 台风轨迹/洪水箭头代码路径从未被真实数据验证（DB 无 cyclone/flood 数据）；(2) metrics 空值须优雅降级，不得 undefined/NaN。代码审查发现两处缺口需补齐。
- **改动**：
  - `web/src/main.js` realGrade：wildfire/flood/volcano/drought/war/crisis_signal/armed_clash 及 default fallback 共 6 类+default 缺 `en` 分支（仅 earthquake/cyclone 有），en 模式下返回中文 grade.text，污染 popup 徽章且经 `${grade.text}` 混入 buildBrief 英文句；逐类补 en 分支，保留中文逻辑不变
  - `web/src/main.js` buildPopupHtml：置信度行 `Number(p.confidence).toFixed(2)` 在 confidence 为 undefined 时产出 "NaN"；改为 `p.confidence != null && Number.isFinite(Number(p.confidence)) ? ... : "-"`，缺失显示 "-"
  - `web/src/main.js` refresh：新增 dev-only `?fixtures=test` 分支（`import.meta.env?.DEV`），加载本地夹具替代 /api/events，手测台风/洪水路径；生产 build 下 DEV=false 不触发，无副作用
  - `web/public/test-fixtures.json`：新建测试夹具，9 类 + 4 边界共 13 条（earthquake M6.5、cyclone M95 带 5 点 footprint、flood M1200、wildfire/volcano/drought/war/crisis_signal/clash 各一；边界：cyclone 无 footprint、flood 无 magnitude、earthquake 无 magnitude、flood 无 confidence/first_seen_at）
- **影响文件/服务**：`web/src/main.js`、`web/public/test-fixtures.json`、`web/dist/`（重新构建）
- **验收**：
  - 代码审查：buildBrief 9 类均有 mag/depth null 兜底（-> "未定"/"n/a"/空串），无 undefined/NaN 泄漏；eqImpactRadiusKm(null)->12、parseMetrics 始终返回对象、fmtNum 调用方均先过滤 null、unitLabel 返回 ""
  - `vp build` 成功：93ms；dist 产物 `index-D4XDvOZv.js` 62.33 kB（gzip 23.10 kB），较改前 +0.68 kB（en 文本+fixtures 分支）；`dist/test-fixtures.json` 已由 public 复制
  - `dist/index.html` 引用更新为 `/assets/index-D4XDvOZv.js`
  - `vp check` 无 type/lint 错误；仅 8 个格式化差异（既有 6 + 本轮新增 main.js/test-fixtures.json 2），遵循 gpt-5.6-terra 既定未跑 `--fix`，建议后续统一处理
- **遗留/风险**：
  - 5173 逐类型手测未做（本机 headless Chrome 不稳定）：建议启动 vp dev 后访问 `http://127.0.0.1:5173/?fixtures=test`，逐一点击各类点位，确认台风轨迹线（fx-cyc-1）/洪水箭头（fx-flood-1，放大 zoom≥5.5）/en 切换后 grade 全英文/confidence 缺失显示"-"
  - test-fixtures.json 经 public 进入 dist（生产含虚构测试数据，几 KB，无安全风险）；若不希望进生产，可在 vite.config.js build 排除或迁移至非 public 路径
  - 未配置 vp test + jsdom 自动单测（需测试基础设施，工作量大）；纯函数降级路径已由代码审查覆盖
  - realGrade i18n 缺口为 grok-4.5 实现遗留，本轮补齐

---

### 2026-08-12 · B2 地图精度与提速复核 + z4->z12 实测 〔glm-5.2〕

- **执行方**：glm-5.2
- **对应任务**：TASK-B2（见第 9 章 PROJECT TODO，已勾选 [x]）
- **动机**：B2 要求复核地图精度（Esri source maxzoom vs map maxZoom）+ 提速配置（refreshExpiredTiles/prefetch/source 重建）+ HARD 实测 z4->z12 耗时与 tile 请求数对比表（基线 vs 改后），不接受「感觉快了」。
- **改动**：
  - `web/src/main.js` SURFACE_TILES：sat/labels maxzoom 19 -> 18（对齐 map maxZoom 18，消除「源支持 z19 而地图限 18 浪费一级」的配置不一致；map maxZoom 18 已限制实际只请求 z18，降 maxzoom 无实际 tile 请求变化）
  - `web/src/main.js` map 配置：新增 `refreshExpiredTiles: false`（静态底图 Esri/OpenTopoMap 不刷新过期瓦片，避免视野回到已加载区域时重复请求）
  - `web/src/main.js` mapReady 后新增 dev-only `window.__crisisMap = map`（`import.meta.env?.DEV`），供测量脚本程序化触发 zoom；生产 build DEV=false 不暴露
  - `web/test/measure-zoom.mjs`：playwright 测量脚本（channel:"chrome" 连系统 Chrome，headless），3 轮 z4->z12，统计 tile 请求数/字节/耗时
- **影响文件/服务**：`web/src/main.js`、`web/test/measure-zoom.mjs`、`web/package.json`（devDeps +playwright）、`web/dist/`（重新构建）
- **验收（实测数据，系统 Chrome headless，3 轮，z4 东京 [139.69,35.69] -> z12）**：
  - 代码审查：sat/labels maxzoom 19 vs map 18（已对齐）；prefetch 默认 true（保留，平移流畅）；maxTileCacheSize 120/fadeDuration 200（可接受）；source 重建无冗余（8 source addSource 初始化 + events/effects setData 更新，无 removeSource）
  - HMR 瞬态：.playwright-cli console log 的 `renderTypeLegend is not defined` 系 HMR 模块重评估瞬态（函数声明提升 + page yml 显示图例正常渲染），非真 bug
  - 基线（改前）：冷启动 36 tiles/151KB，热启动（第2/3轮）0 tiles（cache 命中），idle 事件未触发（12s 兜底）
  - 改后（优化后）：冷启动 36 tiles/143KB，热启动 0-1 tiles，idle 事件未触发
  - 对比：优化前后 z4->z12 首次 tile 请求数无显著变化（36 vs 36）；idle 不触发系持续渲染（livePulse 闪烁动画）所致，非 tile 加载问题，与 refreshExpiredTiles 无关
- **遗留/风险**：
  - 实测结论：z4->z12 是首次加载（cache 冷），`refreshExpiredTiles:false` 针对过期重请求（长期/重复访问场景），不针对首次加载，故首次 tile 数不变（36 vs 36）符合预期；该优化的长期效益（过期 tile 不重请求）需「加载->等待过期->重新访问」场景验证，非 z4->z12 首次
  - idle 事件不适合有持续动画的地图（livePulse 导致持续 render）；如需精确「加载完成时间」，建议改用 sourcedata 事件或停用 livePulse 后测量
  - maxzoom 对齐为配置清理，数据验证无 tile 请求变化（符合预期：map maxZoom 18 已限制）
  - TASK-A7（瓦片缓存代理）触发条件为「B2 实测证明纯前端优化不达标」：本轮实测显示 z4->z12 首次 36 tiles，纯前端优化（refreshExpiredTiles）不针对首次加载，后端代理或可减少跨域/重复请求，但需 grok-4.5 评估是否启动
  - 测试基础设施保留：playwright（devDeps）+ measure-zoom.mjs + window.__crisisMap（dev-only），供后续回归测；diag.mjs 为一次性诊断脚本
  - vp dev 5173 被占，本轮 vp dev 用 5174（已停）

---

### 2026-08-12 · B5 规格 HTML 回写四项偏离 〔glm-5.2〕

- **执行方**：glm-5.2
- **对应任务**：TASK-B5（见第 9 章 PROJECT TODO，已勾选 [x]）
- **动机**：评审报告 c4 章列出四项受控偏离（3.4/3.1/05+06/5.7），规格 HTML 仍是旧文案。规格与实现不一致会让新 Agent 读完规格 3.4 误判 `web/` 为违规产物而删除，或按 5.7 单 DOC 通道实现而破坏 export CSV 兜底。本轮按评审要求回写规格。
- **改动**（6 处，目标文件 `D:\AI\Grok\0811\全球综合危机监测系统-开发执行规格-claude-opus-5.html`）：
  - **3.4 前端依赖**（行 396-400 区）：从「单 HTML + CDN、无构建工具链」回写为 Vite+ 工具链（`vp` v0.2.9，工程 `D:\crisis\web`，scripts dev/build/preview，oxlint/fmt/staged 钩子）；maplibre-gl 锁 5.6.1 仍经 CDN `<script>` 引入为全局 `window.maplibregl`，主逻辑在 `src/main.js`（ES module 由 Vite+ 打包）；deck.gl 注明暂未启用（原计划热点超 5000 时引入，当前规模未到）；加注 9.2 单文件示例仅作历史参考不再有效
  - **3.1 底图决策表**（行 342）：从「Natural Earth GeoJSON 矢量，完全离线」回写为「国界矢量 + 在线卫星/地形栅格瓦片」；写明断网退化行为（国界矢量 Natural Earth 50m 本地 GeoJSON 始终离线可用；Esri/OpenTopoMap 瓦片需联网，断网退化为仅国界矢量 + 事件点）
  - **5.1 源清单**（行 566）：GDELT 行从「DOC 2.0 无坐标」更新为「GDELT 2.0 export CSV 为主、DOC 兜底」；新增 war_hotspots 基线层行（免密钥，13 个持续战区，常态标记非离散事件，GDELT 限流时冲突可见性兜底）
  - **06 章 DDL type 注释**（行 781）：`earthquake|...|crisis_signal` 补 `|war`
  - **08 章 EventType Literal**（行 1112）：补 `"war"`（与 `app/core/schemas.py:9` 实现一致，保持规格内部一致）
  - **5.7 GDELT**（行 723-747 区）：DOC 2.0 单通道回写为「export.CSV 为主 + DOC 兜底」双通道；补 export CSV 流程（lastupdate.txt -> zip -> CSV）、CAMEO 根码 18/19/20 过滤、Actor 行为体类型过滤、ActionGeo_CountryCode FIPS->ISO3->国家质心、MIN_MENTIONS=5；保留国家级定位结论与 sourcecountry 语义陷阱 note
- **影响文件/服务**：`D:\AI\Grok\0811\全球综合危机监测系统-开发执行规格-claude-opus-5.html`（仅规格文档，6 处回写）；未触及 `D:\crisis` 任何代码
- **验收**（评审要求"通读规格 3.1/3.4/05/5.7 章，四项偏离均已回写"）：
  - Grep 验证 6 处回写落位：行 342（3.1 底图）、399（3.4 Vite+）、584（05 war_hotspots）、747（5.7 export.CSV）、812（06 DDL 含 war）、1144（08 EventType 含 war）
  - 实现现状只读确认：`web/package.json`（vite-plus 0.2.9，无 maplibre-gl/deck.gl npm 依赖，CDN 引入）、`web/index.html`（maplibre-gl 5.6.1 CDN script）、`web/src/main.js`（SURFACE_TILES sat/labels/topo + countries.geojson 离线兜底 + deck.gl 无引用）、`app/collectors/war_hotspots.py`（HOTSPOTS 13 战区基线层）、`app/collectors/gdelt.py`（export CSV 为主 DOC 兜底）、`app/core/schemas.py:9`（EventType 含 war）、`setup/05-schema.sql:10-11`（type 注释仍缺 war）
- **遗留/风险**：
  - **06 DDL 注释 vs schema.sql 文件不一致**：规格 06 章 type 注释已补 war，但实际 `setup/05-schema.sql` 文件行 10-11 注释仍缺 war（TASK-A8，grok-4.5 P2 阶段2 待办）。规格回写反映代码现状（schemas.py EventType 已含 war），schema.sql 文件注释未更新是 A8 遗留，转 grok-4.5
  - **9.2 单文件前端示例未删**：规格 9.2（行 2225+）的 `app/static/index.html` 单文件示例与 Vite+ 模块化方案矛盾，本轮仅在 3.4 加注「9.2 仅作历史参考不再有效」未删正文，避免扩大 B5 范围；如需彻底清理 9.2 另议
  - **9.1 离线矢量底图章节未改**：9.1（国界 GeoJSON 下载/入库流程）在实现里仍有效（main.js 仍用 `/data/countries.geojson`），仅 3.1 决策表更新底图策略为「矢量 + 在线瓦片」，9.1 操作流程保留
  - **规格 HTML 字符陷阱**：文件含 em-dash（--，U+2014）与 Unicode 箭标（->，U+2192），Read 工具显示为 ASCII `--`/`->` 导致 Edit old_string 首次未匹配；5.7 区域改用 PowerShell 子串替换（从文件读精确 oldBlock）避免字符误差，其余 5 处用 Edit 成功
  - B5 属 stage 3，但作业范围仅规格 HTML（我的范围），未触及后端代码；war/GDELT 实现状态经只读确认，未修改

### 2026-08-13 · 全项目终审 + A6/A8 修复 + 公网预览 〔grok-4.5〕

- **执行方**：`grok-4.5`
- **对应任务**：对 glm-5.2 B1–B5 评审；全项目复审；TASK-A6 / A8；HTML 终审报告
- **动机**：GLM 阶段完成后需独立复审并自动修复真问题，再提交仓库并提供可打开的大屏公网链接。
- **改动**：
  - A6：`main.py` 改 lifespan；静态目录绝对路径优先 `web/dist`
  - A8：`limit ge=1`；两份 schema 注释补 `war`；`tests/run_unit.py` 7 项通过
  - 战区 upsert 刷新 `occurred_at`；健康检查按源间隔判断 stale
  - 前端：confidence 不再把缺失当成 1；弹窗关闭停地震波；`prefetchZoomDelta:4`；start.ps1 检查 build 退出码
- **验收**：`python -m tests.run_unit` → 7/7；`from app.main import app` 打印静态目录为 `D:\crisis\web\dist`
- **报告**：`docs/终审报告-grok-4.5.html`
